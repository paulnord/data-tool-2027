# Example datasets

**Data… → Examples** has two visible categories: **Synthetic data**, with 14 CSV files, and **Published data**, with 15 choices. Fourteen published choices open configured `.trksess` sessions; the photon-index/temperature example opens a CSV. The categories remain visible while their example lists scroll. Startup remains empty, and choosing an example follows the normal import/review path.

Use **Data… → Load file…** for additional ordinary files. The desktop file chooser initially opens the bundled Examples directory, which also contains prepared synthetic `.trksess` analyses. The same files live in `examples/data/` in the checkout. CSVs can be shared, pasted or used by another analysis program. They contain column headings, explicit units where supplied and observations. Generated CSVs identify their synthetic origin; they do not contain hidden fit settings or executable generators.

## Common workflows

The existing collection covers the main fitting workflows without needing a separate example for every physical application of the same equation.

| Workflow                                          | Existing files                                                                                                                                                                                                                                             | What they demonstrate                                                                                                                                                                              |
| ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Linear motion and calibration                     | [constant-speed.csv](../examples/data/constant-speed.csv), [spring-extension.csv](../examples/data/spring-extension.csv)                                                                                                                                   | Slope and intercept, including a free or fixed force offset.                                                                                                                                       |
| Acceleration                                      | [ball-toss.csv](../examples/data/ball-toss.csv), [prepared session](../examples/data/ball-toss.trksess)                                                                                                                                                    | Quadratic height versus time; acceleration is twice the quadratic coefficient. The session selects the constant-acceleration model.                                                                |
| Unequal measurement uncertainties                 | [unequal-uncertainties.csv](../examples/data/unequal-uncertainties.csv), [prepared session](../examples/data/unequal-weights-demo.trksess)                                                                                                                 | Per-observation Y standard deviations and weighted line fitting. Assign the third CSV column to Y uncertainty explicitly.                                                                          |
| Decay and lifetime parameters                     | [exponential-decay.csv](../examples/data/exponential-decay.csv), [Ba-137m session](../examples/data/published/ba137m-decay.trksess), [ASASSN radio session](../examples/data/published/asassn14li-radio.trksess)                                           | Exponential decay toward a background, or a half-life parameter for background-subtracted counting rates. These are curve fits, not unbinned lifetime or Poisson-likelihood analyses.              |
| Periodic and damped motion                        | [oscillation.csv](../examples/data/oscillation.csv), [damped-sine.csv](../examples/data/damped-sine.csv), [YMnO₃ session](../examples/data/published/ymno3-spin-precession.trksess), [DyFeO₃ session](../examples/data/published/dyfeo3-spin-wave.trksess) | Period, phase and damping, with both synthetic signals and documented published traces.                                                                                                            |
| Peaks and scaling laws                            | [gaussian.csv](../examples/data/gaussian.csv), [lorentzian.csv](../examples/data/lorentzian.csv), [power-law-free.csv](../examples/data/power-law-free.csv)                                                                                                | Peak location and width, and a fitted power-law exponent.                                                                                                                                          |
| Before/after motion and multiple intervals        | [collision.csv](../examples/data/collision.csv), [cart-track.csv](../examples/data/cart-track.csv), [bounce-intervals.csv](../examples/data/bounce-intervals.csv), [oil-drop-intervals.csv](../examples/data/oil-drop-intervals.csv)                       | Four position components for a two-object collision, two cart series, and single-series fits over multiple ranges. All four are synthetic; select the corresponding analysis workspace explicitly. |
| Missing observations and multiple measured series | [MillikanData.csv](../examples/data/MillikanData.csv)                                                                                                                                                                                                      | Supplied oil-drop measurements with gaps and multiple drift intervals. Available through Load file rather than the Examples menu.                                                                  |
| Custom equations and fixed constants              | [Chromium session](../examples/data/published/cri-rydberg.trksess), [BESIII session](../examples/data/published/besiii-ppbarpi0-continuum.trksess), [published index](../examples/data/published/README.md)                                                | Nonlinear equations, fixed physical constants, transformed coordinates and explicit limitations when comparing published results.                                                                  |

Additional synthetic sessions for logarithmic, reciprocal, cubic and quartic fits are available through Load file. Collision and multi-interval examples contain source data, not saved workspace configurations; see [collision scope](collision-draft.md) and [multi-interval scope](multi-interval.md).

`MillikanData.csv` contains columns `t`, `mass_B`, `mass_A`, and `mass_C`, with some missing observations. It can be used to inspect multiple data series and select drift intervals for line fits. Time is in seconds and all three measurement columns are in millimeters, as confirmed by the instructor. Units are declared in the CSV headings; numerical observations and missing values are unchanged.

Loading a CSV does not automatically fit, assert statistical assumptions or assign an uncertainty column. Review the source table and units, then choose X, Y and optional Y uncertainty beside the model in the Analysis panel. For the unequal-uncertainty example, assign the standard-deviation column to Y uncertainty there. Use **Export CSV…** for the table and **Save session** for a reproducible analysis with settings.

Prepared sessions include a selected model, constraints and documented noise provenance. Their observations and settings remain in normal `.trksess` files; no sample is loaded implicitly on startup.

## Provenance and numerical validation

The generated datasets are synthetic. `MillikanData.csv` is an instructor-supplied file, with original numerical observations preserved and instructor-confirmed units added to the headings. The first two are hand-authored examples: constant-speed data are near `position = 0.35 + 1.4*time`; spring data are near `force = 0.1 + 25*extension`. Their small deviations are illustrative and do not establish a Gaussian noise model.

The original CSVs retain observations from the corresponding prepared sessions (except the updated exponential example described below): ball toss uses `height = 2 + 10*time - 4.905*time²`; exponential uses `signal = 0.5 + 3*exp(-0.7*x)`; oscillation uses `y = 1 + 2*sin(2*pi*time/3) + 0.75*cos(2*pi*time/3)`; unequal uncertainties use `y = 1 + 2*x` with `sigma = 0.04 + 0.18*x`. Their original seeds and generation assumptions are in the paired session provenance. Finite noisy samples need not recover the generating coefficients exactly.

Tests read these same static CSV files through production parsing and table validation, check precision and session round trips, and run descriptive fits. The browser suite verifies ordinary file opening and reopening. Internal Monte Carlo generators and scientific acceptance tests remain under `tests/`; they are not exposed as production test controls or included in the application module graph.

## Further nonlinear examples

The same directory now also contains `power-law-free.csv`, `gaussian.csv`, `damped-sine.csv` and `lorentzian.csv`; `exponential-decay.csv` is updated for the fitted-decay-time example. The corresponding models support starting values and fixed or free shape parameters. Peak widths are shape parameters, not measurement uncertainties. See [model equations and diagnostics](nonlinear-fits.md).

These five nonlinear datasets were generated by `scripts/generate-nonlinear-reference.py` with NumPy PCG64 seeds 93000–93004. Their generating parameters, exact observations and independent reference fits are recorded in `tests/fit/nonlinear-reference.json`. The updated exponential example uses `signal = 0.4 + 3*exp(-time/2.2)`; it is separate from the older prepared supplied-rate exponential session.

## Published examples

The `published/` directory contains 14 datasets, each with a CSV, configured session and [individual documentation linked from the comparison index](../examples/data/published/README.md). These explain the physical quantities, column mapping, equations, selected rows, uncertainty conventions and independently reproduced results. Short CSV header notices point to the accompanying page when results differ from the publication. Several differences reflect different methods or datasets; unresolved differences are identified without attributing an error to the authors. The documentation travels with the example files.

The fifteenth Published data menu choice is the separate [pulsar photon-index/temperature CSV](../examples/data/published-pulsar-photon-index-vs-temperature.csv). Its source notice describes an orthogonal-distance regression because both coordinates have uncertainties. Data Tool's Y-error fits do not reproduce that method; preserving the X-error columns does not mean those errors enter its fit.

## Suggested label improvements

These are review recommendations; the menu labels have not been changed by this documentation update. Keep the current two-category structure rather than adding more menus or model-specific choices.

- Match **Damped sine** to the model name **Damped oscillation**, and include “damped” in the two published spin-precession/spin-wave labels.
- Make interval workflows explicit: **Two carts · before and after**, **Bounce · three intervals**, and **Oil-drop drift · three intervals**.
- Restore the measured quantity in the four pulsar/PWN luminosity labels, explain PWN as pulsar wind nebula, and identify **Supercooled-water viscosity (smoothed)** accurately.

Any future switch from a CSV menu entry to a prepared session should preserve the intended observations and document its model and uncertainty settings. In particular, the current exponential-decay CSV and older supplied-rate exponential session are different datasets, as described above. Newly supplied, unpublished measurements should not be described as published data merely to fit an existing category.
