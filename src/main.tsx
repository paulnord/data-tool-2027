import React from "react";
import ReactDOM from "react-dom/client";
import FitApp from "./fit/FitApp";
import "./style.css";
ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <FitApp />
  </React.StrictMode>,
);
